using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockGrab : MonoBehaviour
{
    /*
    RaycastHit hit = this.gameObject.GetComponent("XR Ray Interactor");
    int hover_tag;
    public GameObject[] weapons;

   
   public void ON_Hover()
   {
       if (Weap_Desc.weap)
       {
           hover_tag = int.Parse(hit.collider.tag);
           Debug.Log(hover_tag);
           if (hover_tag >= 0 && hover_tag < 37)
               (weapons[hover_tag].GetComponent("XRGrabInteractable") as MonoBehaviour).enabled = false;
       }
       else
           Debug.Log("Norm");
   }

   public void OUT_Hover()
   {
       if (Weap_Desc.weap)
           (weapons[hover_tag].GetComponent("XRGrabInteractable") as MonoBehaviour).enabled = true;
       else
           Debug.Log("ABC");
   }
   */
}
